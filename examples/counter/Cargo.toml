[package]
name = "counter"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["bevy_log", "bevy_scene", "bevy_asset"]}
bevy-llm-agent = { path = "../../" }
schemars = { workspace = true }
serde = { workspace = true, features = ["derive"] }
