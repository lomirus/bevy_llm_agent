[package]
name = "counter"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["bevy_log"]}
bevy-llm = { path = "../../" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
